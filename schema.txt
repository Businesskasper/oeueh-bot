CREATE TABLE IF NOT EXISTS Reminder (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    message TEXT,
    userId TEXT,
    date INTEGER
);

CREATE TABLE IF NOT EXISTS SoundCommand (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    commandName TEXT,
    filePath TEXT,
    link TEXT
);